{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create New Task</h1>

    {# Show flash messages #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <p><a href="{{ path('todo_list') }}">Back to list</a></p>
{% endblock %}
